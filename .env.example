# Medusa Backend URL (for server-side requests and rewrites)
MEDUSA_BACKEND_URL=http://localhost:9000

# Medusa Public URL (for client-side code if needed, though usually empty string via proxy is preferred)
NEXT_PUBLIC_MEDUSA_URL=http://localhost:9000

# Publishable API Key (from Medusa Admin)
NEXT_PUBLIC_PUBLISHABLE_API_KEY=pk_...
